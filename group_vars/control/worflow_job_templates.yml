---
controller_workflows:
  - name: LEAPP / WORKFLOW / Analysis
    description: Workflow for Leapp Analysis
    # extra_vars:
    #   var_name: value
    inventory: Workshop Inventory
    state: present
    organization: Default
    survey_enabled: true
    survey_spec:
      name: ''
      description: ''
      spec:
        - question_name: Server Name or Pattern
          type: text
          variable: PREFIX
          required: false
    simplified_workflow_nodes:
      - identifier: LEAPP / Snapshot Instance
        unified_job_template: LEAPP / Snapshot Instance
        organization: Default
        all_parents_must_converge: false
        success_nodes:
          - LEAPP / OS Update
      - identifier: LEAPP / OS Update
        unified_job_template: LEAPP / OS Update
        organization: Default
        all_parents_must_converge: false
        success_nodes:
          - LEAPP / Analysis
      - identifier: LEAPP / Analysis
        unified_job_template: LEAPP / Analysis
        organization: Default
        all_parents_must_converge: false
        success_nodes: []
...
